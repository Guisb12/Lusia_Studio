.DS_Store
node_modules/
.idea/

.claude/
.agents/
.env
.env.local
.env*.local

# LUSIA Studio - Mobile is its own Git repo (nested); keep it out of this workspace commit.
# Leading slash: only the top-level Mobile/ folder — avoids ignoring Frontend/components/mobile on case-insensitive FS.
/Mobile/
